# 08 — 配置参考

config.py 全部参数及当前值。

## 设备配置

```python
DEVICE_SERIAL = "127.0.0.1:5555"   # 雷电模拟器 ADB 地址
SCREEN_WIDTH = 1080                 # 游戏原生分辨率
SCREEN_HEIGHT = 1920
SWIPE_DURATION_MS = 100            # 滑动时长（太短变点击，太长延迟高）
CAPTURE_MODE = "minicap"           # minicap | window | adb
```

## 动作空间

```python
ACTION_SPACE = 5    # 0=跳 1=滑 2=左 3=右 4=不动
ACTION_NAMES = ["swipe_up", "swipe_down", "swipe_left", "swipe_right", "no_op"]
```

## 帧/图像

```python
FRAME_WIDTH = 128            # 模型输入分辨率
FRAME_HEIGHT = 128
FRAME_STACK = 4              # 帧堆叠数
INTERVAL = 0.005             # 训练步间隔（秒）
GAME_START_WARMUP_SECONDS = 10.0  # 开局保护（不检测死亡）
USE_GRAYSCALE = False        # 保留 RGB
NUM_LANES = 3                # 左中右三条赛道
LANE_INIT = 2                # 初始在中间
GAME_CROP_LEFT = 60          # 裁剪游戏区域
GAME_CROP_TOP = 200
GAME_CROP_RIGHT = 1020
GAME_CROP_BOTTOM = 1700
```

## PPO 超参

```python
LEARNING_RATE = 3e-4
GAMMA = 0.99
GAE_LAMBDA = 0.95
CLIP_EPSILON = 0.2
ENTROPY_COEF = 0.02
CRITIC_COEF = 0.5
MAX_GRAD_NORM = 0.5
ROLLOUT_STEPS = 512
PPO_EPOCHS = 4
MINI_BATCH_SIZE = 64
```

## 奖励

```python
REWARD_ALIVE = 0.5     # 存活奖励
REWARD_DEATH = -10.0   # 死亡惩罚
REWARD_DANGER = -0.3   # 障碍逼近（帧差法）
DANGER_FRAME_DIFF_THRESH = 8.0
```

## 死亡检测

```python
HEART_REGION = (0,0)-(120,50)    # 红心区域
HEART_H_MIN1/MAX1 = 0/10         # H 红色范围1
HEART_H_MIN2/MAX2 = 170/180      # H 红色范围2
HEART_S_MIN = 80                 # 饱和度
HEART_V_MIN = 80                 # 亮度
HEART_PIXEL_RATIO = 0.005        # 红心占比阈值
HEART_DEATH_FRAMES = 10          # 连续丢失帧数
GAME_OFFSET_X = 0                # 侧边栏偏移（minicap下为0）
GAME_OFFSET_Y = 0
```

## 按钮坐标（1080×1920）

```python
GIVE_UP_BUTTON = (134, 1850)     # 放弃/取消
RESTART_BUTTON = (544, 1800)     # 重新开始
START_BUTTON = (698, 1491)       # 开始跑酷
AUTO_LOCATE_BUTTONS = False      # 自动定位（关闭）
STEP1_DELAY = 1.0                # 重启间隔
STEP2_DELAY = 1.0
STEP3_DELAY = 1.5
```

## 模型保存

```python
MODEL_DIR = "models"
CHECKPOINT_PATH = "models/checkpoint.pth"
BEST_MODEL_PATH = "models/best_model.pth"
TEMP_MODEL_DIR = "models/temp"
SAVE_INTERVAL = 30               # 每30轮保存
BEST_SAVE_WINDOW = 20            # 最佳模型窗口
```

## 预训练

```python
PRETRAIN_EPOCHS = 30
PRETRAIN_BATCH = 128
PRETRAIN_LR = 1e-3
```

## 数据采集

```python
DATA_DIR = "data"
NOOP_OBSERVE_INTERVAL = 0.4     # 空闲录no-op间隔
MIN_ACTION_INTERVAL = 0.5        # 训练用操作间隔
```
